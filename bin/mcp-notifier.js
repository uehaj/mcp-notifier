#!/usr/bin/env node

// This is a simple CLI wrapper for the MCP Notifier server
import '../build/index.js';

// The main server code will be executed when imported
console.error('MCP Notifier CLI wrapper initialized');
